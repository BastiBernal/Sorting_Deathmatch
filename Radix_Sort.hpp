#ifndef RADIX_SORT_HPP
#define RADIX_SORT_HPP

#include <vector>

// Función para ordenar un vector usando Radix Sort (modificado para manejar números negativos)
void radixSortWithNegatives(std::vector<int>& arr);

#endif // RADIX_SORT_HPP